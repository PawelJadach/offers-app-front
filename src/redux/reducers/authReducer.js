const initState = {
  user: {
    name: 'janek'
  },
  error: '',
}

const authReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default authReducer;